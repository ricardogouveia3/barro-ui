import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { colors } from './colors';
import { typography } from './typography';

<Meta title="Design Tokens/Colors & Typography" />

# Design Tokens

Barro UI uses a set of design tokens to ensure consistency across the application. These tokens are available as TypeScript constants in `lib/tokens`.

## Colors

The color palette is inspired by the raw beauty of Pernambuco's clay art and culture.

<div style={{ display: 'flex', flexDirection: 'column', gap: '2rem' }}>
  <div>
    <h3 style={{ color: '#fff', marginBottom: '1rem' }}>Quartz - Primary grays and neutrals</h3>
    <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(120px, 1fr))', gap: '1rem' }}>
      {Object.entries(colors.quartz).map(([shade, color]) => (
        <div key={shade} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div
            style={{
              width: '100px',
              height: '100px',
              backgroundColor: color,
              borderRadius: '8px',
              border: '1px solid rgba(255, 255, 255, 0.1)',
            }}
          />
          <div style={{ marginTop: '0.5rem', fontSize: '0.875rem', color: '#fff' }}>{shade}</div>
          <div style={{ fontSize: '0.75rem', color: '#999' }}>{color}</div>
        </div>
      ))}
    </div>
  </div>

  <div>
    <h3 style={{ color: '#fff', marginBottom: '1rem' }}>Base - Black and White</h3>
    <div style={{ display: 'flex', gap: '1rem' }}>
      {Object.entries(colors.base).map(([name, color]) => (
        <div key={name} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div
            style={{
              width: '100px',
              height: '100px',
              backgroundColor: color,
              borderRadius: '8px',
              border: '1px solid rgba(255, 255, 255, 0.1)',
            }}
          />
          <div style={{ marginTop: '0.5rem', fontSize: '0.875rem', color: '#fff', textTransform: 'capitalize' }}>{name}</div>
          <div style={{ fontSize: '0.75rem', color: '#999' }}>{color}</div>
        </div>
      ))}
    </div>
  </div>
</div>

## Typography

<div className="sb-unstyled" style={{ color: '#fff' }}>
  <h3>Font Family</h3>
  <div
    style={{
      fontFamily: typography.fontFamily.sans.join(', '),
      fontSize: '1.5rem',
      marginBottom: '1rem',
    }}
  >
    {typography.fontFamily.sans.join(', ')}
  </div>
  <p>The quick brown fox jumps over the lazy dog.</p>
</div>
